# Full Harness compaction observability

The bridge logs one trace/thread correlation record for each Web response turn so diagnostics can associate native compaction, semantic Web summarization, and the first resumed browser turn without changing compaction policy.
